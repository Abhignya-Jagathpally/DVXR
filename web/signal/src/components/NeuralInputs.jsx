import ModalityInputGroup from "./ModalityInputGroup.jsx";

export default function NeuralInputs({ inputs, onChange }) {
  return <ModalityInputGroup groupKey="neural" inputs={inputs} onChange={onChange} />;
}
